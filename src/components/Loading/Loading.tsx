import * as React from 'react';

const Loading = () => (
    <div className="fixed bg-black top-0 left-0 w-full h-full z-99 opacity-50 flex items-center justify-center">
        <span className="text-white">Loading...</span>
    </div>
);

export default Loading;
